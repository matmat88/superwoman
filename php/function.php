
<?php
// CONSTANTE => constante
        // $nombreAleatoire => variable
        // localhost/php/index.php?nombre=5
        // $_GET => tableau associatif qui représente tout ce qui est dans la chaine de requête
        // $_GET:[`nombre`=>5] nombre=> clé  $_GET["nombre"] équivaut à 5
        // $t=[1,2,3,4] => tableau simple   $t[1] equivaut à 2

//  php lotterie
// JS Math.random => [0,1[ => PHP rand($min,$max)=>nombre entier
//  JS t.length => PHP count($t)
//  JS t.includes('x') => PHP in_array($valeur,$t)
//  JS t.push(x) => PHP $t[]=$valeur 
// rand($min,$max);
// count($t);
// in_array($valeur,$t);
// $t[]=$valeur;
// array_key_exists('z',$t);
// array_search();
// array_flip();

// // php language de type explicite
// function f (int $x,int $y) : int
// {
//     return $y +$y;
// };

// // fonction de verification
// // => required en html
// // pattern=> expression régulière
// isset($task['title']); /*=> verifie que la clé titre éxiste*/
// throw new \Exception("Les données ne sont pas valides", 102);
// try{}
// catch(\Exception $e){
//     echo "<p>Une erreur a empêché l'exécution de votre requête : ".$e->getMessage()." </p>";
// }


?>